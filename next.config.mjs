/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com', 'api.microlink.io', 'img.freepik.com'],
    },
  };
  
  export default nextConfig;
  